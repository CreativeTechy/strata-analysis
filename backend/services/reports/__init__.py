"""Reports-page Export Summary PDF: one consistent report-data snapshot
(report_data.py), the LLM day-over-day comparison built on top of it
(yesterday_comparison.py), and the PyMuPDF renderer that lays both out
(pdf_renderer.py)."""
