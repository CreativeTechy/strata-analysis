# Auto-read by `npm run build` (Vite production mode). Not a secret — public URL.
# This makes Spider Mode always point at the HF Space, so a plain `npm run build`
# can't accidentally drop it.
VITE_SPIDER_URL=https://hsnmm-strata-spider.hf.space
